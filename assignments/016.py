# Create a function rectangle_area that takes two parameters,
# length and width (with width defaulting to 1), and returns
# the area of the rectangle.
def rectangle_area(length, width=1):
    return length * width


print(rectangle_area(2, 3))
print(rectangle_area(2))
